extends Node

static var C : Dictionary[Enum.ChordType, ChordResource] = {
	
}

static var Cs : Dictionary[Enum.ChordType, ChordResource] = {
	
}

static var D : Dictionary[Enum.ChordType, ChordResource] = {
	
}

static var Ds : Dictionary[Enum.ChordType, ChordResource] = {
	
}

static var E : Dictionary[Enum.ChordType, ChordResource] = {
	
}

static var F : Dictionary[Enum.ChordType, ChordResource] = {
	
}

static var Fs : Dictionary[Enum.ChordType, ChordResource] = {
	
}

static var G : Dictionary[Enum.ChordType, ChordResource] = {
	Enum.ChordType.MAJOR : preload("res://scenes/chords/g/gmaj.tres")
}

static var Gs : Dictionary[Enum.ChordType, ChordResource] = {
	
}

static var A : Dictionary[Enum.ChordType, ChordResource] = {
	
}

static var As : Dictionary[Enum.ChordType, ChordResource] = {
	
}

static var B : Dictionary[Enum.ChordType, ChordResource] = {
	
}
